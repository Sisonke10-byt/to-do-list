apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: to-do-list

resources:
  - namespace.yaml
  - postgres-secret.yaml
  - postgres-pvc.yaml
  - postgres-statefulset.yaml
  - postgres-service.yaml
  - app-config.yaml
  - app-secret.yaml
  - app-deployment.yaml
  - app-service.yaml

commonLabels:
  app.kubernetes.io/name: to-do-list
  app.kubernetes.io/version: 0.0.1
